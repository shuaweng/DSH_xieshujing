# 兼容矩阵

| 写书鲸 | DeepSeek Harness | Node.js | 状态 |
|---|---|---|---|
| `0.1.2-alpha.7` | `0.1.2-alpha.2` 版本家族 | `^22.19.0 || >=24.0.0` | 支持 |

写书鲸使用精确的 DSH peer dependency 版本家族共享 Cordis、Agent、Session 和 Client runtime。不要使用 `--force` 跳过 peer dependency 冲突；请升级到与当前 DSH 版本匹配的写书鲸标签。

写书鲸和 DSH 仍处于预发布阶段。每个发布标签只承诺与本表列出的 DSH 版本家族兼容。
