import { Service } from "@deepseek-ai/cordis";
import z from "@deepseek-ai/schemastery";
import { isSessionSkillEnabled } from "@deepseek-ai/dsh-tool-skill";
//#region lib/types/noai.js
/** Deterministic Chinese web-fiction style heuristics adapted from the shipped Novel guard. */
const RULES = [
	{
		id: "explanatory-dash",
		label: "解释性破折号",
		severity: "high",
		advice: "删掉作者式补充定义，改成动作、反应、对白或直接结果。",
		patterns: [/——\s*(?:也就是|也就是说|换句话说|说白了|准确地说|其实|本质上|所谓|即|因为|为了)[^。\n！？!?；;]{1,80}/gu]
	},
	{
		id: "not-but-structure",
		label: "否定转折模板",
		severity: "high",
		advice: "直接写动作、判断或后果，避免“不是……而是……”绕圈下定义。",
		patterns: [/(?:不是|没有)[^。\n！？!?；;]{0,50}而是/gu, /不仅[^。\n！？!?；;]{0,50}(?:更|而且|还)/gu]
	},
	{
		id: "not-like-structure",
		label: "不像/倒像模板",
		severity: "medium",
		advice: "用现场动作和可感知细节代替作者站出来解释比喻。",
		patterns: [/不像是?在?[^。\n！？!?；;]{1,40}[，,]?倒像是?在?[^。\n！？!?；;]{1,60}/gu]
	},
	{
		id: "ratio-emotion",
		label: "比例式情绪描写",
		severity: "high",
		advice: "改成具体表情、动作或更贴角色的一句对白。",
		patterns: [/(?:眼神|目光|脸上|表情|神色)[^。\n！？!?；;]{0,18}[一二三四五六七八九十\d]分[^。\n！？!?；;]{1,30}[一二三四五六七八九十\d]分[^。\n！？!?；;]{1,40}/gu]
	},
	{
		id: "expression-template",
		label: "表情模板句",
		severity: "medium",
		advice: "优先写动作选择、停顿、语气变化或对方反应。",
		patterns: [/嘴角(?:勾起|扯出|扬起|泛起)[^。\n！？!?；;]{0,18}(?:一抹|一丝)[^。\n！？!?；;]{0,36}/gu, /眼神中?(?:闪过|掠过|浮现|写满|透着)[^。\n！？!?；;]{0,42}/gu]
	},
	{
		id: "stage-transition",
		label: "影视分镜式转场词",
		severity: "low",
		advice: "密集出现时删去提示词，用因果动作自然衔接。",
		patterns: [
			/(?:就在)?下一秒[，,]?/gu,
			/与此同时[，,]?/gu,
			/紧接着[，,]?/gu,
			/时间仿佛在这一刻静止/gu
		]
	},
	{
		id: "expository-reveal",
		label: "设定揭示式旁白",
		severity: "high",
		advice: "把总结拆进动作、对白、界面信息或冲突后果。",
		patterns: [/这就是[^。\n！？!?；;]{1,50}真相/gu, /真正可怕的是[^。\n！？!?；;]{1,80}/gu]
	},
	{
		id: "authorial-stamp",
		label: "作者旁白盖章",
		severity: "high",
		advice: "删去“意味着/标志着/注定”等盖章，让读者从结果自行判断。",
		patterns: [/这(?:一刻|一下|一招|件事)?[^。\n]{0,18}(?:意味着|标志着|象征着|注定|将会|会彻底改变)[^。\n]{1,80}/gu]
	},
	{
		id: "omniscient-insert",
		label: "开天眼式身份塞入",
		severity: "high",
		advice: "若当前视角不知情，改用衣饰、称呼、旁人反应或对方自报。",
		patterns: [/这是[^。\n]{1,60}(?:道上人称|人称|外门|小头目|头目|身份|名叫|叫作|乃是)[^。\n]{0,40}/gu]
	},
	{
		id: "empty-abstraction",
		label: "抽象情绪与气氛词堆叠",
		severity: "medium",
		advice: "补充可见动作、声音、物件变化或人物即时选择。",
		patterns: [/(?:一种|某种)(?:难以言喻|莫名|说不清|无法形容)的[^。\n！？!?；;]{1,36}/gu, /空气中弥漫着[^。\n！？!?；;]{1,50}(?:气息|氛围|味道)/gu]
	},
	{
		id: "repeated-emphasis",
		label: "重复强调式短排比",
		severity: "high",
		advice: "删掉作者式二次盖章，把信息压回角色当前能感知到的动作、疼痛、选择或处境。",
		patterns: [/[他她它我][^。\n！？!?]{0,12}是个[^。\n！？!?]{1,24}[。\n！？!?]\s*一个[^。\n！？!?]{1,50}/gu, /[他她它我][^。\n！？!?]{0,12}只是[^。\n！？!?]{1,24}[。\n！？!?]\s*一个[^。\n！？!?]{1,50}/gu]
	},
	{
		id: "anaphora-stack",
		label: "连续排比式自我强调",
		severity: "medium",
		advice: "连续“没有/不是/更没有”容易像模板化强调；保留最有画面的一个结果。",
		patterns: [/(?:^|\n)\s*没有[^。\n！？!?]{0,30}[。\n！？!?]\s*\n\s*没有[^。\n！？!?]{0,30}[。\n！？!?](?:\s*\n\s*(?:更)?没有[^。\n！？!?]{0,35}[。\n！？!?])?/gu, /(?:^|\n)\s*不是[^。\n！？!?]{0,30}[。\n！？!?]\s*\n\s*不是[^。\n！？!?]{0,30}[。\n！？!?]/gu]
	},
	{
		id: "quote-hedging",
		label: "引号包裹俗语或概念",
		severity: "medium",
		advice: "若不是实物标签、书名、引用或角色命名，删掉引号或换成更具体的表达。",
		patterns: [/[“"][^”"\n]{0,8}(?:拳风|气势|惊天|震撼|冷漠|残暴|不可理解|仙法|规矩|节目效果|眼前一亮)[^”"\n]{0,8}[”"]/gu]
	},
	{
		id: "logic-chain-exposition",
		label: "规则推演式解释",
		severity: "medium",
		advice: "保留理解情节所需的规则，其余用试探动作、现场反馈和后果呈现。",
		patterns: [
			/按照(?:剧本|规则|系统判定)[：:][^。\n！？!?；;]{1,140}/gu,
			/(?:直接|绕过|强行|现在)[^。\n！？!?；;]{1,36}[？?]?不行[，,][^。\n！？!?；;]{1,100}/gu,
			/必须让一切发生得[^。\n！？!?；;]{1,100}/gu
		]
	},
	{
		id: "perfect-logic-claim",
		label: "完美判定或完美扮演",
		severity: "medium",
		advice: "改成实际判定结果、失败风险或旁观者反应，不让系统替文本鼓掌。",
		patterns: [/(?:完美|100%|百分百)[^。\n！？!?；;]{0,36}(?:符合|通过|人设|逻辑|判定|扮演)/gu, /极其自然[，,]顺理成章/gu]
	},
	{
		id: "detached-voice-camera",
		label: "脱离人物视角的声音镜头",
		severity: "medium",
		advice: "让声音落到视角人物的听觉、身体反应或现场动作上。",
		patterns: [/[“"][^”"\n]{4,80}[”"]\s*(?:\n\s*)?一个[^。\n]{0,24}声音[^。\n]{0,40}(?:炸开|响起|传来)/gu, /一个[^。\n]{0,24}声音[^。\n]{0,40}(?:炸开|响起|传来)/gu]
	},
	{
		id: "pov-camera-switch",
		label: "导演镜头式视角切换",
		severity: "high",
		advice: "除非明确切换视角，否则改成当前视角能观察到的动作和反应。",
		patterns: [/[他她它][^。\n]{0,6}看得很清楚[:：][^。\n]{1,80}/gu, /他们[^。\n]{0,8}(?:甚至不知道|只看到)[^。\n]{1,80}/gu]
	},
	{
		id: "technique-explainer",
		label: "作者式技巧或机制解释",
		severity: "medium",
		advice: "除非是明确的角色判断，否则用可见后果代替作者贴出的说明牌。",
		patterns: [/(?:这|那)是[^。\n！？!?；;]{1,60}(?:技巧|规矩|经验|手段|办法|忌讳|排异感|代价|后遗症)[^。\n！？!?；;]{0,60}/gu, /(?:犯了|坏了)[^。\n！？!?；;]{0,24}(?:规矩|忌讳)[^。\n！？!?；;]{0,60}/gu]
	},
	{
		id: "action-evaluation",
		label: "动作后作者评价",
		severity: "medium",
		advice: "把动作后的抽象评价换成对手反应、身体代价或战术结果。",
		patterns: [/(?:这一连串动作|这一刀|这一拳|这一击)[^。\n！？!?；;]{0,30}(?:快得离谱|精准|残忍|霸道|狠辣)[^。\n！？!?；;]{0,80}/gu, /(?:透着|带着)一种令人(?:胆寒|窒息|心悸|发毛|头皮发麻)[^。\n！？!?；;]{0,50}/gu]
	},
	{
		id: "explanation-marker",
		label: "解释或结论先行",
		severity: "medium",
		advice: "先写现象、反应和代价，再判断是否还需要解释。",
		patterns: [/(?:原因很简单|本质上|这说明|这代表|这意味着)[^。\n]{1,80}/gu]
	},
	{
		id: "generic-pain-imagery",
		label: "通用痛感或暴力比喻",
		severity: "medium",
		advice: "痛感比喻密集时改成身体失控、呼吸变化、可见动作或战术后果。",
		patterns: [/(?:像|如同)[^。\n]{0,16}(?:野狗|风箱|破麻袋|刀子|重锤|烂泥|死人|铁钉|蚂蚁|岩浆|凌迟)[^。\n]{0,40}/gu, /(?:潮水般|地狱般|非人(?:的)?力道|令人胆寒的平静)[^。\n]{0,40}/gu]
	},
	{
		id: "abstract-emotion-label",
		label: "抽象情绪标签",
		severity: "medium",
		advice: "不要替角色命名情绪，改成身体反应、动作失控、停顿或贴角色的对白。",
		patterns: [
			/[他她它我][^。\n！？!?；;]{0,10}(?:感到|觉得|心中|心里|内心)[^。\n！？!?；;]{0,8}(?:一[阵丝股])?(?:难受|崩溃|绝望|震惊|恐惧|愤怒|痛苦|心痛|心碎|慌乱|焦虑|无助|屈辱|悲伤|惊恐)/gu,
			/[他她它我][^。\n！？!?；;]{0,10}(?:感到|觉得|感受到|心中|心里|内心|心头)[^。\n！？!?；;]{0,8}(?:愕然|错愕|释然|欣慰|激动)/gu,
			/一(?:种|股|阵)[^。\n！？!?；;]{0,12}(?:绝望|崩溃|恐惧|愤怒|屈辱|悲凉|无力|窒息)(?:感|的情绪|的感觉)/gu
		]
	},
	{
		id: "low-specificity-scenery",
		label: "低具体度描写",
		severity: "low",
		advice: "用一个可感的具体细节替代笼统形容词与抽象气氛词。",
		patterns: [/(?:古朴|斑驳|陈旧|破旧|神秘|诡异|压抑|阴森|宁静|祥和|繁华|萧条|肃穆)的(?:客栈|房间|屋子|院子|建筑|街道|气息|气氛|氛围|感觉|地方)/gu, /空气中(?:弥漫|充斥|飘荡|流动)着[^。\n！？!?；;]{0,20}(?:气息|寒意|压抑|诡异|危险|不安|紧张)/gu]
	},
	{
		id: "enemy-cognition-overreach",
		label: "对手认知越界",
		severity: "medium",
		advice: "把对手脑内活动改成当前视角可见的物理反应。",
		patterns: [/(?:对方|敌人|对面那?[人个]?|那几[人个]|那些人|众人|对面)[^。\n！？!?；;]{0,12}(?:无法理解|想不通|看不懂|不明白|无法想象|完全没料到|怎么也想不到|做梦也想不到|心中认定|早已看穿|早就看穿)/gu]
	},
	{
		id: "when-clause-opener",
		label: "当…时叙事开场",
		severity: "medium",
		advice: "改成直接动作、具象场景或角色当下的选择。",
		patterns: [/(?:^|\n)\s*(?![“"「『])(?:当|每当|正当|就在)[^。！？!?；;\n]{1,24}(?:时|的时候)[，,][^。！？!?；;\n]{2,44}[。！？!?]/gu]
	},
	{
		id: "markdown-residue",
		label: "正文中的 Markdown 残留",
		severity: "high",
		advice: "小说正文不使用 Markdown 标题、加粗或列表标记；删除格式符号并保留正文。",
		patterns: [
			/(?:^|\n)\s{0,3}#{1,6}\s+[^\n]+/gu,
			/\*\*[^*\n]{1,80}\*\*/gu,
			/(?:^|\n)\s*[-*+]\s+[^\n]+/gu
		]
	},
	{
		id: "promotional-vocabulary",
		label: "宣传式与高频 AI 词汇",
		severity: "medium",
		advice: "删除抽象宣传词，直接写可感知事实和后果。",
		patterns: [/(?:值得注意的是|毫无疑问|至关重要|深入探讨|令人印象深刻|充满活力|丰富的底蕴|令人叹为观止|不可磨灭的印记)/gu]
	},
	{
		id: "overqualification",
		label: "过度限定",
		severity: "medium",
		advice: "保留一个必要的不确定词，删除其余回避性限定。",
		patterns: [/(?:或许|可能|也许)[^。\n]{0,18}(?:可能|或许|也许|某种程度|一定程度)[^。\n]{0,40}/gu]
	},
	{
		id: "generic-positive-ending",
		label: "通用积极结尾",
		severity: "medium",
		advice: "用尚未解决的问题、具体行动或不确定后果收尾，不替章节总结希望。",
		patterns: [/(?:虽然前路|尽管前路)[^。\n]{0,60}(?:终有一天|充满希望|希望之火|迎接未来)[^。\n]{0,80}[。！？!?]?\s*$/gu, /(?:眼中|心中)[^。\n]{0,20}(?:重新)?燃起了?(?:不灭的)?希望(?:之火)?[^。\n]{0,30}[。！？!?]?\s*$/gu]
	}
];
const SEVERITY_WEIGHT = {
	high: 3,
	medium: 2,
	low: 1
};
/**
* Scan exact prose with deterministic, source-offset-preserving rules.
* @param text - exact Asset model text or materialized ChangeSet candidate.
* @param options - provider-owned sample and result bounds.
* @returns a stable JSON-safe report; insufficient samples remain explicit.
*/
function scanNoAi(text, options) {
	validateOptions(options);
	let characterCount = 0;
	for (const character of text) if (!/\s/u.test(character)) characterCount += 1;
	const sampleLevel = characterCount < options.minCharacters ? "insufficient" : characterCount < options.strongSampleCharacters ? "usable" : "strong";
	const findings = [];
	const seen = /* @__PURE__ */ new Set();
	for (const rule of RULES) for (const pattern of rule.patterns) {
		pattern.lastIndex = 0;
		for (const match of text.matchAll(pattern)) {
			const start = match.index;
			const evidence = match[0];
			if (evidence.length === 0) continue;
			const key = `${rule.id}:${start}:${evidence.length}`;
			if (seen.has(key)) continue;
			seen.add(key);
			findings.push({
				ruleId: rule.id,
				label: rule.label,
				severity: rule.severity,
				startUtf16: start,
				endUtf16: start + evidence.length,
				evidence,
				advice: rule.advice
			});
		}
	}
	findings.sort((left, right) => SEVERITY_WEIGHT[right.severity] - SEVERITY_WEIGHT[left.severity] || left.startUtf16 - right.startUtf16 || left.ruleId.localeCompare(right.ruleId));
	const counts = findings.reduce((result, finding) => {
		result[finding.severity] += 1;
		return result;
	}, {
		high: 0,
		medium: 0,
		low: 0
	});
	const weighted = findings.reduce((total, finding) => total + SEVERITY_WEIGHT[finding.severity], 0);
	const densityBase = Math.max(1, characterCount / 1e3);
	const riskScore = sampleLevel === "insufficient" ? 0 : Math.min(100, Math.round(weighted * 8 / densityBase));
	return {
		version: 1,
		characterCount,
		sampleLevel,
		riskScore,
		counts,
		findings: findings.slice(0, options.maxFindings)
	};
}
function validateOptions(options) {
	for (const [name, value] of Object.entries(options)) if (!Number.isSafeInteger(value) || value < 1) throw new TypeError(`novel-analysis: ${name} must be a positive integer`);
	if (options.strongSampleCharacters < options.minCharacters) throw new TypeError("novel-analysis: strongSampleCharacters must not be smaller than minCharacters");
}
//#endregion
//#region lib/types/index.js
/** Revision-bound deterministic scans and fixed chapter-review orchestration. */
const NOAI_ANALYZER_VERSION = "noai-rules/2";
const REVIEW_ANALYZER_VERSION = "chapter-review/2";
const PREFERENCE_EXTRACTOR_VERSION = "final-preference/1";
const STORY_STATE_EXTRACTOR_VERSION = "story-state/1";
const DEFAULT_MIN_CHARACTERS = 300;
const DEFAULT_STRONG_SAMPLE_CHARACTERS = 1200;
const DEFAULT_MAX_FINDINGS = 80;
const DEFAULT_WARN_RISK_SCORE = 50;
const DEFAULT_WARN_HIGH_FINDINGS = 3;
/** Host coordinator for exact-Revision scans and read-only chapter review. */
var NovelAnalysis = class extends Service {
	static inject = [
		"fs",
		"novelRepository",
		"novelAssetTypes",
		"novelContextResolver",
		"sandboxPolicy",
		"subagents"
	];
	static Config = z.object({
		subagentProvider: z.string().default("spawn"),
		noAiMinCharacters: z.number().step(1).min(1).default(DEFAULT_MIN_CHARACTERS),
		noAiStrongSampleCharacters: z.number().step(1).min(1).default(DEFAULT_STRONG_SAMPLE_CHARACTERS),
		noAiMaxFindings: z.number().step(1).min(1).default(DEFAULT_MAX_FINDINGS),
		noAiWarnRiskScore: z.number().step(1).min(1).max(100).default(DEFAULT_WARN_RISK_SCORE),
		noAiWarnHighFindings: z.number().step(1).min(1).default(DEFAULT_WARN_HIGH_FINDINGS)
	});
	config;
	constructor(ctx, config = {}) {
		super(ctx, "novelAnalysis");
		this.config = resolveConfig(config);
	}
	/**
	* Deterministically scan and persist one exact chapter Revision.
	* @param agent - owning Session used to locate and authorize the Novel Project.
	* @param assetId - exact manuscript chapter identity.
	* @param revisionId - retained Revision to scan.
	* @param signal - optional caller cancellation before persistence.
	* @returns the upserted exact-Revision report.
	*/
	async scanChapter(agent, assetId, revisionId, signal) {
		const project = await this.resolveProject(agent, signal);
		const snapshot = await this.ctx.novelRepository.readAsset(project, assetId, revisionId, signal);
		assertChapter(snapshot);
		const report = scanNoAi(this.ctx.novelAssetTypes.get(snapshot.asset.type).modelText(snapshot), noAiOptions(this.config));
		return await this.ctx.novelRepository.putAnalysisReport(project, {
			assetId,
			revisionId,
			kind: "noai-scan",
			analyzerVersion: NOAI_ANALYZER_VERSION,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			data: report,
			sourceSessionId: agent.id
		}, signal);
	}
	/**
	* Run the fixed read-only chapter reviewer and persist valid structured output.
	* @param agent - owning root Agent and review provenance.
	* @param assetId - exact manuscript chapter identity.
	* @param revisionId - retained Revision to review.
	* @param signal - canonical cancellation for worker startup and execution.
	* @returns the upserted exact-Revision review.
	*/
	async reviewChapter(agent, assetId, revisionId, signal) {
		if (!isSessionSkillEnabled(agent.session, "chapter-review")) throw new Error("novel analysis: chapter-review Skill is disabled for this Session");
		const project = await this.resolveProject(agent, signal);
		const assets = await this.ctx.novelRepository.listAssets(project, signal, this.ctx.sandboxPolicy.resolve({ session: agent.session }));
		const chapter = await this.ctx.novelRepository.readAsset(project, assetId, revisionId, signal);
		assertChapter(chapter);
		const title = assets.find((value) => value.asset.id === assetId)?.title ?? "未命名章节";
		const compiled = await this.ctx.novelContextResolver.compile(agent, {
			policies: ["chapter-review"],
			targets: [{
				projectId: project.id,
				assetId: chapter.asset.id,
				revisionId: chapter.revisionId,
				label: title,
				origin: "active-asset",
				mode: "explicit",
				projection: "full",
				reason: "target-asset",
				required: true
			}],
			includeWorkset: true
		}, signal);
		const noAi = scanNoAi(this.ctx.novelAssetTypes.get(chapter.asset.type).modelText(chapter), noAiOptions(this.config));
		const prompt = `先调用 skill 加载 chapter-review 方法，然后对 Novel Context Compiler 冻结的材料执行严格审稿。你的职责是挑错，不是鼓励作者；禁止礼貌性夸奖、空泛肯定和默认高分。必须完整提交八个维度，id 依次覆盖 plot、logic、character、pacing、hook、style、immersion、ai-pattern，评分为 0 到 100 的整数且不得重复。90 分以上仅用于几乎无需修改的罕见成稿；80 分代表仍有明确缺陷；70 分代表存在影响阅读的问题；60 分以下代表需要实质重写。逐项检查剧情推进、逻辑与连续性、人物可信度、节奏、追读钩子、文风表达、出戏点、AI 味与模板化。finding 必须给正文中的准确短引文、具体诊断和可执行修法；不要限制为少量问题，也不要把同一问题拆成凑数条目。不得修改任何资产。\n\n下面的确定性 NOAI 扫描只提供候选证据；你必须结合上下文复核，成立则写入 ai-pattern finding，不成立则不要照抄：\n<deterministic-noai>${JSON.stringify({
			analyzerVersion: NOAI_ANALYZER_VERSION,
			sampleLevel: noAi.sampleLevel,
			riskScore: noAi.riskScore,
			counts: noAi.counts,
			findings: noAi.findings.slice(0, 16).map((finding) => ({
				ruleId: finding.ruleId,
				label: finding.label,
				severity: finding.severity,
				evidence: finding.evidence,
				advice: finding.advice
			}))
		})}</deterministic-noai>\n\n${compiled.text}`;
		const run = await this.ctx.subagents.start(this.config.subagentProvider, {
			label: `章节审稿 · ${title}`,
			parent: agent,
			signal,
			prompt: [{
				type: "text",
				text: prompt
			}],
			maxDepth: 1,
			toolFilter: { allow: ["skill"] },
			persona: "你是苛刻、克制、证据导向的只读网络小说责任编辑。你的工作是系统性找出文本中的缺陷，而不是附和作者。凡是 AI 模板腔、出戏表达、逻辑漏洞、人物失真、节奏拖沓、信息重复和无效钩子，都要用原文证据指出。不要为了显得友善抬高分数，不写礼貌性夸奖；不得改稿或声称资产已修改。",
			outputSchema: REVIEW_SCHEMA
		});
		const result = await settleRun(run);
		if (result.stopReason !== "completed" || result.structured === void 0) throw new Error(`novel analysis: chapter reviewer ended with ${result.stopReason}`);
		const review = decodeReview(result.structured);
		return await this.ctx.novelRepository.putAnalysisReport(project, {
			assetId,
			revisionId,
			kind: "chapter-review",
			analyzerVersion: REVIEW_ANALYZER_VERSION,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			data: review,
			sourceSessionId: agent.id,
			workerSessionId: run.id
		}, signal);
	}
	/**
	* Retain explicit finalization, then infer one inert preference candidate when evidence exists.
	*/
	async finalizeChapter(agent, assetId, revisionId, signal) {
		const project = await this.resolveProject(agent, signal);
		const finalization = await this.ctx.novelRepository.finalizeRevision(project, assetId, revisionId, agent.id, signal);
		let story;
		try {
			story = await this.extractStoryStateCandidate(agent, project, assetId, revisionId, signal);
		} catch {
			signal.throwIfAborted();
			story = { storyCandidateError: "extraction-failed" };
		}
		if (finalization.sourceRevisionId === void 0) return {
			finalization,
			...story,
			noCandidateReason: "no-agent-source"
		};
		if (finalization.sourceRevisionId === revisionId) return {
			finalization,
			...story,
			noCandidateReason: "no-author-diff"
		};
		const existing = await this.ctx.novelRepository.listPreferenceCandidates(project, assetId, revisionId, signal);
		if (existing[0] !== void 0) return {
			finalization,
			...story,
			candidate: existing[0]
		};
		const styleSummary = (await this.ctx.novelRepository.listAssets(project, signal, this.ctx.sandboxPolicy.resolve({ session: agent.session }))).find((value) => sameRuntimeType(value.asset.type, "book.style-profile"));
		if (styleSummary === void 0) return {
			finalization,
			...story,
			noCandidateReason: "missing-style-profile"
		};
		const source = await this.ctx.novelRepository.readAsset(project, assetId, finalization.sourceRevisionId, signal);
		const final = await this.ctx.novelRepository.readAsset(project, assetId, revisionId, signal);
		assertChapter(source);
		assertChapter(final);
		if (this.ctx.novelAssetTypes.get(source.asset.type).modelText(source) === this.ctx.novelAssetTypes.get(final.asset.type).modelText(final)) return {
			finalization,
			...story,
			noCandidateReason: "no-author-diff"
		};
		const style = await this.ctx.novelRepository.readAsset(project, styleSummary.asset.id, styleSummary.revisionId, signal);
		const material = `先调用 skill 加载 preference-learning 方法。只提炼作者从 Agent 草稿改到定稿时反复可迁移的表达、节奏、对白或信息释放偏好；剧情事实、人名、地点和本章偶然事件不得写入长期偏好。输出严格结构化结果。guidanceMarkdown 必须是可以追加到本书风格中的简洁 Markdown，不重复已有规则；evidence 提供 1 到 8 组准确短证据。\n\n${(await this.ctx.novelContextResolver.compile(agent, {
			policies: ["preference-learning"],
			targets: [
				{
					projectId: project.id,
					assetId: source.asset.id,
					revisionId: source.revisionId,
					label: "Agent 草稿",
					origin: "search",
					mode: "explicit",
					projection: "full",
					reason: "draft-source",
					required: true
				},
				{
					projectId: project.id,
					assetId: final.asset.id,
					revisionId: final.revisionId,
					label: "作者定稿",
					origin: "active-asset",
					mode: "explicit",
					projection: "full",
					reason: "final-source",
					required: true
				},
				{
					projectId: project.id,
					assetId: style.asset.id,
					revisionId: style.revisionId,
					label: styleSummary.title,
					origin: "search",
					mode: "explicit",
					projection: "full",
					reason: "book-style",
					required: true
				}
			]
		}, signal)).text}`;
		const result = await settleRun(await this.ctx.subagents.start(this.config.subagentProvider, {
			label: `定稿偏好提取 · ${assetId}`,
			parent: agent,
			signal,
			prompt: [{
				type: "text",
				text: material
			}],
			maxDepth: 1,
			toolFilter: { allow: ["skill"] },
			persona: "你是只读的作者偏好分析员。只比较给定 Agent 草稿与作者显式定稿，提炼可迁移且有证据的文风、节奏或表达偏好；不得把剧情事实当风格，不得修改资产。",
			outputSchema: PREFERENCE_SCHEMA
		}));
		if (result.stopReason !== "completed" || result.structured === void 0) throw new Error(`novel analysis: preference extractor ended with ${result.stopReason}`);
		const extracted = decodePreference(result.structured);
		const candidate = await this.ctx.novelRepository.putPreferenceCandidate(project, {
			assetId,
			sourceRevisionId: source.revisionId,
			finalRevisionId: final.revisionId,
			...finalization.sourceChangeSetId === void 0 ? {} : { sourceChangeSetId: finalization.sourceChangeSetId },
			...finalization.sourceSessionId === void 0 ? {} : { sourceSessionId: finalization.sourceSessionId },
			targetStyleAssetId: style.asset.id,
			targetStyleRevisionId: style.revisionId,
			extractorVersion: PREFERENCE_EXTRACTOR_VERSION,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			...extracted
		}, signal);
		return {
			finalization,
			...story,
			candidate
		};
	}
	async extractStoryStateCandidate(agent, project, assetId, revisionId, signal) {
		const existing = await this.ctx.novelRepository.listStoryStateCandidates(project, assetId, revisionId, signal);
		if (existing[0] !== void 0) return { storyCandidate: existing[0] };
		const assets = await this.ctx.novelRepository.listAssets(project, signal, this.ctx.sandboxPolicy.resolve({ session: agent.session }));
		const storySummary = assets.find((value) => sameRuntimeType(value.asset.type, "book.story-state"));
		if (storySummary === void 0) return { noStoryCandidateReason: "missing-story-state" };
		const chapter = await this.ctx.novelRepository.readAsset(project, assetId, revisionId, signal);
		assertChapter(chapter);
		const storyState = await this.ctx.novelRepository.readAsset(project, storySummary.asset.id, storySummary.revisionId, signal);
		const prompt = `先调用 skill 加载 story-state-extraction 方法。根据定稿正文更新当前 Story State。只记录正文已经确立的事实，不把大纲计划、推测或修辞当成事实；保留与本章无关的现有状态，更新已改变的状态。replacementMarkdown 必须是可直接替换整个 Story State 的完整、简洁 Markdown。evidence 提供 1 到 12 条定稿正文中的准确短引文及其支持的状态变化。不得修改任何资产。\n\n${(await this.ctx.novelContextResolver.compile(agent, {
			policies: ["story-state-learning"],
			targets: [{
				projectId: project.id,
				assetId: chapter.asset.id,
				revisionId: chapter.revisionId,
				label: assets.find((value) => value.asset.id === chapter.asset.id)?.title ?? "定稿章节",
				origin: "active-asset",
				mode: "explicit",
				projection: "full",
				reason: "final-source",
				required: true
			}, {
				projectId: project.id,
				assetId: storyState.asset.id,
				revisionId: storyState.revisionId,
				label: storySummary.title,
				origin: "search",
				mode: "explicit",
				projection: "full",
				reason: "story-state",
				required: true
			}]
		}, signal)).text}`;
		const run = await this.ctx.subagents.start(this.config.subagentProvider, {
			label: `故事状态提取 · ${assetId}`,
			parent: agent,
			signal,
			prompt: [{
				type: "text",
				text: prompt
			}],
			maxDepth: 1,
			toolFilter: { allow: ["skill"] },
			persona: "你是只读的长篇小说 Story State 管理员。只从给定定稿正文中提取已发生、已确认且对后续创作有用的状态变化；不得把未来计划写成现实，不得修改资产。",
			outputSchema: STORY_STATE_SCHEMA
		});
		const result = await settleRun(run);
		if (result.stopReason !== "completed" || result.structured === void 0) throw new Error(`novel analysis: Story State extractor ended with ${result.stopReason}`);
		const extracted = decodeStoryState(result.structured);
		return { storyCandidate: await this.ctx.novelRepository.putStoryStateCandidate(project, {
			assetId,
			finalRevisionId: revisionId,
			targetStoryStateAssetId: storyState.asset.id,
			targetStoryStateRevisionId: storyState.revisionId,
			extractorVersion: STORY_STATE_EXTRACTOR_VERSION,
			generatedAt: (/* @__PURE__ */ new Date()).toISOString(),
			workerSessionId: run.id,
			...extracted
		}, signal) };
	}
	/** Apply one reviewed candidate to the exact style Revision through ChangeSet publication. */
	async acceptPreference(agent, candidateId, signal) {
		const project = await this.resolveProject(agent, signal);
		const candidate = await this.ctx.novelRepository.readPreferenceCandidate(project, candidateId, signal);
		if (candidate.status !== "pending") throw new Error("novel analysis: preference candidate is already terminal");
		const style = await this.ctx.novelRepository.readAsset(project, candidate.targetStyleAssetId, candidate.targetStyleRevisionId, signal);
		const definition = this.ctx.novelAssetTypes.get(style.asset.type);
		const body = definition.modelText(style);
		const operations = definition.prepareOperations(style, [{
			kind: "replace-text",
			startUtf16: body.length,
			endUtf16: body.length,
			replacement: `

## 从定稿中确认的写作偏好

${candidate.guidanceMarkdown.trim()}\n`
		}]);
		const proposed = await this.ctx.novelRepository.proposeChangeSet(project, {
			assetId: style.asset.id,
			baseRevisionId: style.revisionId,
			operations,
			actor: {
				kind: "user",
				sessionId: agent.id
			},
			summary: `采纳定稿偏好：${candidate.summary}`
		}, signal);
		const applied = await this.ctx.novelRepository.applyChangeSet(project, proposed.id, { sessionId: agent.id }, signal, this.ctx.sandboxPolicy.resolve({ session: agent.session }));
		if (applied.status !== "applied" || applied.resultRevisionId === void 0) return {
			candidate,
			changeSet: applied
		};
		return {
			candidate: await this.ctx.novelRepository.decidePreferenceCandidate(project, candidate.id, "accepted", agent.id, {
				changeSetId: applied.id,
				revisionId: applied.resultRevisionId
			}, signal),
			changeSet: applied
		};
	}
	/** Retain explicit rejection without changing authored assets. */
	async rejectPreference(agent, candidateId, signal) {
		const project = await this.resolveProject(agent, signal);
		return await this.ctx.novelRepository.decidePreferenceCandidate(project, candidateId, "rejected", agent.id, void 0, signal);
	}
	/** Apply one reviewed complete Story State replacement through ChangeSet publication. */
	async acceptStoryState(agent, candidateId, signal) {
		const project = await this.resolveProject(agent, signal);
		const candidate = await this.ctx.novelRepository.readStoryStateCandidate(project, candidateId, signal);
		if (candidate.status !== "pending") throw new Error("novel analysis: Story State candidate is already terminal");
		const storyState = await this.ctx.novelRepository.readAsset(project, candidate.targetStoryStateAssetId, candidate.targetStoryStateRevisionId, signal);
		const definition = this.ctx.novelAssetTypes.get(storyState.asset.type);
		const body = definition.modelText(storyState);
		const operations = definition.prepareOperations(storyState, [{
			kind: "replace-text",
			startUtf16: 0,
			endUtf16: body.length,
			replacement: `${candidate.replacementMarkdown.trim()}\n`
		}]);
		const proposed = await this.ctx.novelRepository.proposeChangeSet(project, {
			assetId: storyState.asset.id,
			baseRevisionId: storyState.revisionId,
			operations,
			actor: {
				kind: "user",
				sessionId: agent.id
			},
			summary: `采纳定稿故事状态：${candidate.summary}`
		}, signal);
		const applied = await this.ctx.novelRepository.applyChangeSet(project, proposed.id, { sessionId: agent.id }, signal, this.ctx.sandboxPolicy.resolve({ session: agent.session }));
		if (applied.status !== "applied" || applied.resultRevisionId === void 0) return {
			candidate,
			changeSet: applied
		};
		return {
			candidate: await this.ctx.novelRepository.decideStoryStateCandidate(project, candidate.id, "accepted", agent.id, {
				changeSetId: applied.id,
				revisionId: applied.resultRevisionId
			}, signal),
			changeSet: applied
		};
	}
	/** Retain explicit Story State rejection without changing authored assets. */
	async rejectStoryState(agent, candidateId, signal) {
		const project = await this.resolveProject(agent, signal);
		return await this.ctx.novelRepository.decideStoryStateCandidate(project, candidateId, "rejected", agent.id, void 0, signal);
	}
	/**
	* Scan one proposal candidate and render bounded deferred model feedback.
	* @param base - exact ChangeSet base snapshot.
	* @param operations - type-validated operations already accepted for proposal.
	* @returns material warning, or `undefined` for non-chapters, small samples, or low risk.
	*/
	candidateWarning(base, operations) {
		if (base.asset.type !== "manuscript.chapter") return void 0;
		const definition = this.ctx.novelAssetTypes.get(base.asset.type);
		const materialized = definition.materializeOperations(base, operations);
		const candidate = {
			...base,
			serializedUtf8: materialized.serializedUtf8,
			frontmatter: materialized.parsed.frontmatter,
			content: materialized.parsed.content
		};
		const report = scanNoAi(definition.modelText(candidate), noAiOptions(this.config));
		if (report.sampleLevel === "insufficient") return void 0;
		if (report.riskScore < this.config.warnRiskScore && report.counts.high < this.config.warnHighFindings) return void 0;
		const top = report.findings.slice(0, 5).map((item) => `- ${item.label}（${item.severity}）${JSON.stringify(item.evidence)}：${item.advice}`).join("\n");
		return {
			report,
			text: `<novel-noai-candidate-warning>\n这份正文 ChangeSet 候选已创建，但确定性 NOAI 扫描发现较高模板化风险（risk ${report.riskScore}/100，高风险 ${report.counts.high} 处）。\n${top}\n请在回复用户前说明这些风险；不要声称提案已应用，也不要自动另建提案。\n</novel-noai-candidate-warning>`
		};
	}
	async resolveProject(agent, signal) {
		const cwd = agent.session.header.cwd;
		if (cwd === void 0) throw new Error("novel analysis: Session has no Novel Project working directory");
		const root = await this.ctx.fs.resolve(cwd, {
			cwd,
			...signal === void 0 ? {} : { signal }
		});
		const project = await this.ctx.novelRepository.discoverProject(root, signal);
		if (project === void 0) throw new Error("novel analysis: Session working directory is not a Novel Project");
		return project;
	}
};
const REVIEW_SCHEMA = {
	type: "object",
	additionalProperties: false,
	properties: {
		sampleLevel: {
			type: "string",
			enum: [
				"insufficient",
				"usable",
				"strong"
			]
		},
		overallScore: { type: "integer" },
		verdict: { type: "string" },
		dimensions: {
			type: "array",
			items: {
				type: "object",
				additionalProperties: false,
				properties: {
					id: {
						type: "string",
						enum: [
							"plot",
							"logic",
							"character",
							"pacing",
							"hook",
							"style",
							"immersion",
							"ai-pattern"
						]
					},
					score: { type: "integer" },
					summary: { type: "string" }
				},
				required: [
					"id",
					"score",
					"summary"
				]
			}
		},
		findings: {
			type: "array",
			items: {
				type: "object",
				additionalProperties: false,
				properties: {
					severity: {
						type: "string",
						enum: [
							"high",
							"medium",
							"low"
						]
					},
					category: { type: "string" },
					quote: { type: "string" },
					diagnosis: { type: "string" },
					suggestion: { type: "string" }
				},
				required: [
					"severity",
					"category",
					"quote",
					"diagnosis",
					"suggestion"
				]
			}
		},
		priorities: {
			type: "array",
			items: { type: "string" }
		}
	},
	required: [
		"sampleLevel",
		"overallScore",
		"verdict",
		"dimensions",
		"findings",
		"priorities"
	]
};
const PREFERENCE_SCHEMA = {
	type: "object",
	additionalProperties: false,
	properties: {
		summary: { type: "string" },
		guidanceMarkdown: { type: "string" },
		evidence: {
			type: "array",
			items: {
				type: "object",
				additionalProperties: false,
				properties: {
					before: { type: "string" },
					after: { type: "string" },
					inference: { type: "string" }
				},
				required: [
					"before",
					"after",
					"inference"
				]
			}
		}
	},
	required: [
		"summary",
		"guidanceMarkdown",
		"evidence"
	]
};
const STORY_STATE_SCHEMA = {
	type: "object",
	additionalProperties: false,
	properties: {
		summary: { type: "string" },
		replacementMarkdown: { type: "string" },
		evidence: {
			type: "array",
			items: {
				type: "object",
				additionalProperties: false,
				properties: {
					quote: { type: "string" },
					update: { type: "string" }
				},
				required: ["quote", "update"]
			}
		}
	},
	required: [
		"summary",
		"replacementMarkdown",
		"evidence"
	]
};
function resolveConfig(config) {
	const resolved = {
		subagentProvider: config.subagentProvider ?? "spawn",
		minCharacters: config.noAiMinCharacters ?? DEFAULT_MIN_CHARACTERS,
		strongSampleCharacters: config.noAiStrongSampleCharacters ?? DEFAULT_STRONG_SAMPLE_CHARACTERS,
		maxFindings: config.noAiMaxFindings ?? DEFAULT_MAX_FINDINGS,
		warnRiskScore: config.noAiWarnRiskScore ?? DEFAULT_WARN_RISK_SCORE,
		warnHighFindings: config.noAiWarnHighFindings ?? DEFAULT_WARN_HIGH_FINDINGS
	};
	if (resolved.subagentProvider.length === 0 || resolved.subagentProvider !== resolved.subagentProvider.trim()) throw new TypeError("novel-analysis: subagentProvider must be a normalized non-empty string");
	for (const [name, value] of Object.entries(resolved).filter(([, value]) => typeof value === "number")) if (!Number.isSafeInteger(value) || value < 1) throw new TypeError(`novel-analysis: ${name} must be a positive integer`);
	if (resolved.strongSampleCharacters < resolved.minCharacters) throw new TypeError("novel-analysis: strong sample threshold must not be smaller than minimum sample");
	if (resolved.warnRiskScore > 100) throw new TypeError("novel-analysis: warnRiskScore must not exceed 100");
	return resolved;
}
function noAiOptions(config) {
	return {
		minCharacters: config.minCharacters,
		strongSampleCharacters: config.strongSampleCharacters,
		maxFindings: config.maxFindings
	};
}
function assertChapter(snapshot) {
	if (snapshot.asset.type !== "manuscript.chapter") throw new Error("novel analysis: target must be manuscript.chapter");
}
function sameRuntimeType(value, expected) {
	return typeof value === "string" && value === expected;
}
async function settleRun(run) {
	let result;
	let primary;
	try {
		result = await run.result;
	} catch (error) {
		primary = error;
	}
	try {
		await run.dispose();
	} catch (error) {
		if (primary !== void 0) throw new AggregateError([primary, error], "novel analysis: reviewer and disposal failed");
		throw error;
	}
	if (primary instanceof Error) throw primary;
	if (primary !== void 0) throw new Error("novel analysis: reviewer failed", { cause: primary });
	if (result === void 0) throw new Error("novel analysis: reviewer produced no terminal result");
	return result;
}
function isRecord(value) {
	return typeof value === "object" && value !== null && !Array.isArray(value);
}
function normalizedText(value, max = 4e3) {
	return typeof value === "string" && value.length > 0 && value.length <= max && value === value.trim();
}
function decodeReview(value) {
	if (!isRecord(value) || ![
		"insufficient",
		"usable",
		"strong"
	].includes(String(value["sampleLevel"])) || !Number.isSafeInteger(value["overallScore"]) || Number(value["overallScore"]) < 0 || Number(value["overallScore"]) > 100 || !normalizedText(value["verdict"]) || !Array.isArray(value["dimensions"]) || value["dimensions"].length !== 8 || !Array.isArray(value["findings"]) || value["findings"].length > 50 || !Array.isArray(value["priorities"]) || value["priorities"].length > 12) throw new Error("novel analysis: chapter reviewer returned a malformed report");
	const dimensions = value["dimensions"].map((item) => {
		if (!isRecord(item) || ![
			"plot",
			"logic",
			"character",
			"pacing",
			"hook",
			"style",
			"immersion",
			"ai-pattern"
		].includes(String(item["id"])) || !Number.isSafeInteger(item["score"]) || Number(item["score"]) < 0 || Number(item["score"]) > 100 || !normalizedText(item["summary"], 1e3)) throw new Error("novel analysis: reviewer dimension is invalid");
		return {
			id: item["id"],
			score: item["score"],
			summary: item["summary"]
		};
	});
	if (new Set(dimensions.map((item) => item.id)).size !== dimensions.length) throw new Error("novel analysis: reviewer dimensions must be unique");
	const findings = value["findings"].map((item) => {
		if (!isRecord(item) || ![
			"high",
			"medium",
			"low"
		].includes(String(item["severity"])) || !normalizedText(item["category"], 200) || !normalizedText(item["quote"], 500) || !normalizedText(item["diagnosis"], 1500) || !normalizedText(item["suggestion"], 1500)) throw new Error("novel analysis: reviewer finding is invalid");
		return {
			severity: item["severity"],
			category: item["category"],
			quote: item["quote"],
			diagnosis: item["diagnosis"],
			suggestion: item["suggestion"]
		};
	});
	const priorities = value["priorities"].map((item) => {
		if (!normalizedText(item, 1e3)) throw new Error("novel analysis: reviewer priority is invalid");
		return item;
	});
	return {
		version: 1,
		sampleLevel: value["sampleLevel"],
		overallScore: value["overallScore"],
		verdict: value["verdict"],
		dimensions,
		findings,
		priorities
	};
}
function decodePreference(value) {
	if (!isRecord(value) || !normalizedText(value["summary"], 1e3) || !normalizedText(value["guidanceMarkdown"], 8e3) || !Array.isArray(value["evidence"]) || value["evidence"].length < 1 || value["evidence"].length > 8) throw new Error("novel analysis: preference extractor returned malformed output");
	const evidence = value["evidence"].map((item) => {
		if (!isRecord(item) || !normalizedText(item["before"], 1e3) || !normalizedText(item["after"], 1e3) || !normalizedText(item["inference"], 1e3)) throw new Error("novel analysis: preference evidence is invalid");
		return {
			before: item["before"],
			after: item["after"],
			inference: item["inference"]
		};
	});
	return {
		summary: value["summary"],
		guidanceMarkdown: value["guidanceMarkdown"],
		evidence
	};
}
function decodeStoryState(value) {
	if (!isRecord(value) || !normalizedText(value["summary"], 1e3) || !normalizedText(value["replacementMarkdown"], 64e3) || !Array.isArray(value["evidence"]) || value["evidence"].length < 1 || value["evidence"].length > 12) throw new Error("novel analysis: Story State extractor returned malformed output");
	const evidence = value["evidence"].map((item) => {
		if (!isRecord(item) || !normalizedText(item["quote"], 1e3) || !normalizedText(item["update"], 1e3)) throw new Error("novel analysis: Story State evidence is invalid");
		return {
			quote: item["quote"],
			update: item["update"]
		};
	});
	return {
		summary: value["summary"],
		replacementMarkdown: value["replacementMarkdown"],
		evidence
	};
}
//#endregion
export { NovelAnalysis, NovelAnalysis as default, scanNoAi };
